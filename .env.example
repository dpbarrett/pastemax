# Example .env file for PasteMax

# API Keys for Web-Based AI Models (Optional)
# Obtain these manually from the respective providers.
# Store actual keys in a `.env.local` file (excluded from version control).
# Ensure secure handling of these keys as per security rules.

# Required for integrating with OpenAI API (e.g., for token counting or future model interactions)
# Obtain your API key from https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Required for integrating with Anthropic API (if using Claude models)
# Obtain your API key from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Add other model API keys as needed

# Default Settings (Optional - can also be managed via electron-store)
# Default maximum file size in bytes (e.g., 5MB)
DEFAULT_MAX_FILE_SIZE=5242880
# PASTEMAX_DATA_DIR="/path/to/custom/data"

# Remember to remove the '#' (uncomment) the variables you are setting in your actual .env file.



# Note: This is a template. Do not use this file directly. Create a `.env.local` file for your local settings.
# Ensure to keep your API keys secure and do not expose them in public repositories.