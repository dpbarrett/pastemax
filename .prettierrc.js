module.exports = {
    // Use single quotes instead of double quotes
    singleQuote: true,
    // Add a semicolon at the end of statements
    semi: true,
    // Print trailing commas wherever possible in multi-line comma-separated syntax structures
    trailingComma: 'all',
    // Specify the line length that the printer will wrap on
    printWidth: 100,
    // Indent lines with tabs or spaces
    useTabs: false,
    // Specify the number of spaces per indentation-level
    tabWidth: 2,
    // Include parentheses around a sole arrow function parameter
    arrowParens: 'always',
    // Include the project's .editorconfig settings if present (optional)
    // editorconfig: true,
    // Specify the end of line sequence
    endOfLine: 'lf'
};